version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd portal
        - npm ci --include=dev
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: portal/dist
    files:
      - '**/*'
  cache:
    paths:
      - portal/node_modules/**/*
